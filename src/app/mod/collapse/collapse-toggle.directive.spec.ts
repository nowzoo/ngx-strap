import { CollapseToggleDirective } from './collapse-toggle.directive';

describe('CollapseToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseToggleDirective();
    expect(directive).toBeTruthy();
  });
});
