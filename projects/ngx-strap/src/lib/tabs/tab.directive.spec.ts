import { NgxStrapTabDirective } from './tab.directive';

describe('TabDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxStrapTabDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
