import { DropdownDirective } from './dropdown.directive';

describe('DropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownDirective({} as any, {} as any, {} as any);
    expect(directive).toBeTruthy();
  });
});
