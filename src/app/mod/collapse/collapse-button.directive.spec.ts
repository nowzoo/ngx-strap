import { CollapseButtonDirective } from './collapse-button.directive';

describe('CollapseButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseButtonDirective();
    expect(directive).toBeTruthy();
  });
});
