import { NgxStrapTabTitleDirective } from './tab-title.directive';

describe('NgxStrapTabTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxStrapTabTitleDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
